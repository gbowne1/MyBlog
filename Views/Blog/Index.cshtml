@{
    ViewData["Title"] = "Blog";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>@ViewData["Title"]</h1>

@foreach (var post in Model)
{
    <div>
        <h2>@post.Title</h2>
        <p>@post.Content</p>
    </div>
}
